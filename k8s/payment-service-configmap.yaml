apiVersion: v1
kind: ConfigMap
metadata:
  name: payment-service-config
data:
  SERVER_PORT: "8082"
  SPRING_DATASOURCE_URL: "jdbc:postgresql://userdatabase.postgres.database.azure.com:5432/paymendb?sslmode=require"

  # Clerk JWT
  CLERK_ISSUER: "https://upright-bird-25.clerk.accounts.dev"
  CLERK_JWKS_URL: "https://upright-bird-25.clerk.accounts.dev/.well-known/jwks.json"
  JWT_DEV_MODE: "false"

  # Cross-service URLs (cluster DNS)
  POST_SERVICE_BASE_URL: "http://post-service:8081"
  USER_SERVICE_BASE_URL: "http://user-service:8080"

  # Web3 / Polygon
  POLYGON_RPC_URL: "https://polygon-rpc.com"

  # CoinGecko (non-secret bits)
  COINGECKO_API_KEY_HEADER: "x-cg-demo-api-key"
  COINGECKO_BASE_URL: "https://api.coingecko.com/api/v3"
