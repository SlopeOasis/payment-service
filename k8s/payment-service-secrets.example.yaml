apiVersion: v1
kind: Secret
metadata:
  name: payment-service-secrets
type: Opaque
stringData:
  # Put your real CoinGecko key here (do NOT commit the real version of this file).
  COINGECKO_API_KEY: "REPLACE"
